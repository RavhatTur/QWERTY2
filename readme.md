Вопросы 
1. Понятие постоянной продвинутой угрозы (APT). Примеры. 
— 
Постоянная продвинутая угроза (APT) - это целенаправленная и продолжительная кибератака, которая обычно осуществляется крупными киберпреступными группами. APT-атаки направлены на получение конфиденциальной информации, нарушение работы системы или внедрение вредоносного программного обеспечения. Пример: Атаки Deep Panda – группировка, вероятно, из Китая провела атаку на Отдел по управлению персоналом при правительстве США, скомпрометировав более 4 миллионов записей, которые могли содержать подробности о сотрудниках спецслужб.
—

2. Понятие проактивного поиска угроз информационной безопасности (cyber threat hunting). Определение, задачи, основные средства. Общая последовательность этапов проактивного поиска угроз информационной безопасности. 
— 
Проактивный поиск угроз информационной безопасности (cyber threat hunting) - это процесс активного поиска потенциальных угроз и аномалий в компьютерных системах и сетях, который может не быть обнаружен автоматическими средствами безопасности. Основная задача cyber threat hunting - предотвратить или минимизировать воздействие угроз на систему. Основные средства включают специализированные программы для обнаружения аномалий, мониторинга сетевого трафика и анализа журналов событий. Общая последовательность этапов проактивного поиска угроз информационной безопасности включает определение целей, сбор данных, анализ данных, выявление угроз и принятие мер по их устранению.
 —

3. Модели зрелости процесса поиска угроз информационной безопасности. 
— 
Модели зрелости процесса поиска угроз информационной безопасности - это систематический подход к развитию и оценке уровня зрелости процесса поиска угроз в организации. Примеры моделей зрелости включают модель SEI (Software Engineering Institute) и модель MITRE ATT&CK (Adversarial Tactics, Techniques, and Common Knowledge). Эти модели оценивают различные аспекты процесса поиска угроз, такие как организационная структура, методы и инструменты 
—

4. Язык программирования R. Основные типы данных. Вектор. Перевод из одних типов данных в другие. 
— 
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. Основные типы данных в R включают числа (integer, numeric), логические значения (logical), символы (character) и факторы (factor). Вектор - это основная структура данных в R, которая может содержать элементы одного типа. Перевод из одного типа данных в другой можно осуществить с помощью функций, таких как as.integer(), as.character(), as.logical() и т. д.
—

5. Язык программирования R. Основные типы данных. Датафрейм. Перевод из одних типов данных в другие.
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. Основные типы данных в R включают числа (integer, numeric), логические значения (logical), символы (character) и факторы (factor).Датафрейм - это структура данных, которая представляет собой двумерную таблицу с переменными (столбцами) разных типов данных. Перевод из одного типа данных в другой можно осуществить с помощью функций, таких как as.integer(), as.character(), as.logical() 
—

6. Язык программирования R. Функции, оператор конвейера. Конвейер в пакете dplyr. Примеры. 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. Конвейер в пакете dplyr - это последовательное применение функций к данным с помощью оператора %>% (pipe). Примеры использования конвейера в пакете dplyr включают комбинирование операций фильтрации, сортировки и группировки данных.
—

7. Язык программирования R. Функции sapply() , lapply() . 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. Функция sapply() применяет функцию к каждому элементу списка или вектора и возвращает результат в виде вектора, а функция lapply() возвращает результат в виде списка.
—

8. Язык программирования R. Аналитическая экосистема Tidyverse, общие подходы, состав. 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. Аналитическая экосистема Tidyverse - это набор пакетов R, которые обеспечивают согласованный и удобный для использования набор функций для работы с данными. Основные подходы в Tidyverse включают работу с данными в формате dataframe, использование оператора конвейера %>% (pipe) для последовательного применения функций и акцент на читаемости и понятности кода. Состав Tidyverse включает пакеты dplyr, tidyr, ggplot2, readr.
—

9. Язык программирования R. Применение функции dplyr::mutate() 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. mutate() - это функция из пакета dplyr, которая добавляет новые столбцы к существующему датафрейму или изменяет значения существующих переменных. Функция mutate() принимает в качестве аргументов датафрейм и спецификацию новых переменных.
—

10. Язык программирования R. Применение функции dplyr::filter() 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. filter() - это функция из пакета dplyr, которая фильтрует строки датафрейма на основе заданных условий. Функция filter() принимает в качестве аргументов датафрейм и условия фильтрации, указанные с помощью операторов сравнения (например, ==, >, <) и логических операторов (например, & - логическое "и", | - логическое "или").
—

11. Язык программирования R. Применение функции dplyr::select() 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. select() - это функция из пакета dplyr, которая выбирает определенные переменные (столбцы) из датафрейма. Функция select() принимает в качестве аргументов датафрейм и список переменных, которые нужно выбрать. Можно использовать различные операторы для указания переменных
—

12. Язык программирования R. Импорт данных, чтение данных из файла. 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. Одним из наиболее популярных пакетов является readr. Вот примеры кода: 

Чтение данных из CSV файла
data <- read_csv("file.csv")

Чтение данных из Excel файла
data <- read_excel("file.xlsx")

Чтение данных из базы данных SQLite
data <- read_sqlite("database.sqlite", "table_name")
—

13. Язык программирования R. Сохранение рабочей области, запись данных в файл. 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных.
Сохранение рабочей области в файл
save.image("workspace.RData")

Запись данных в CSV файл
write_csv(data, "file.csv")

Запись данных в Excel файл
write_excel(data, "file.xlsx")

Запись данных в базу данных SQLite
write_sqlite(data, "database.sqlite", "table_name")
—

14. Язык программирования R. Визуализация данных: основные средства, примеры.
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных.
Для визуализации данных в R можно использовать пакет  ggplot2
Пример кода:
ggplot(data, aes(x = category, y = value)) +
  geom_bar(stat = "identity")
Функция ggplot(), которая создает новый объект графика. Внутри функции мы указываем данные, которые будут использоваться для построения графика (data), а также задаем эстетику (aes - aesthetics), то есть указываем, какие переменные будут использоваться для определения положения столбцов по оси x (x = category) и высоты столбцов по оси y (y = value).

geom_bar(stat = "identity") - это слой графика, который определяет тип геометрии, в данном случае столбчатую диаграмму. Параметр stat = "identity" указывает, что высота каждого столбца будет определяться непосредственно значениями переменной value в данных.

+ - это оператор, который используется для добавления слоев на график.
—

15. Язык программирования R. Пакет readr . Основные функции пакета readr . 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных.
read_csv(): чтение данных из файла CSV.
read_tsv(): чтение данных из файла TSV (текстовый файл с табуляцией в качестве разделителя).
read_excel(): чтение данных из файла Excel.
read_delim(): чтение данных из текстового файла с пользовательским разделителем.
read_fwf(): чтение данных из текстового файла с фиксированной шириной полей.
—

16. Язык программирования R. Пакет stringr . Основные функции пакета stringr . 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных.
Пакет stringr предоставляет функции для работы со строками в R. Некоторые из них:
str_detect(): определение, содержит ли строка указанный шаблон.
str_replace(): замена первого вхождения шаблона в строке.
str_replace_all(): замена всех вхождений шаблона в строке.
str_sub(): извлечение подстроки из строки.
str_split(): разделение строки на подстроки.
—

17. Язык программирования R. Пакет purrr . Основные функции пакета purrr . 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных.
Пакет purrr предоставляет функции для работы с коллекциями данных в R
map(): применение функции к каждому элементу коллекции.
map2(): применение функции к соответствующим элементам двух коллекций.
reduce(): последовательное применение функции к элементам коллекции.
filter(): фильтрование элементов коллекции на основе заданного условия.
mutate(): создание новой переменной на основе существующих переменных.
—

18. Язык программирования R. Пакет httr2 . Основные функции пакета httr2 . 
—
Язык программирования R - это язык и среда для статистического анализа и визуализации данных. 
В R пакет httr2 является клиентской библиотекой HTTP, которая предоставляет удобные инструменты для отправки HTTP-запросов и обработки HTTP-ответов. Этот пакет обрабатывает различные аспекты взаимодействия с веб-серверами, такие как создание и отправка запросов, обработка заголовков, установка параметров запроса, обработка авторизации и т.д.
GET(): отправка GET-запроса.
POST(): отправка POST-запроса.
PUT(): отправка PUT-запроса.
DELETE(): отправка DELETE-запроса.
HEAD(): отправка HEAD-запроса.

GET(): Функция GET() используется для отправки HTTP GET-запроса на указанный URL. GET-запросы используются для получения данных с сервера. Вы можете указать дополнительные параметры в запросе, такие как заголовки, параметры URL или аутентификацию. Функция возвращает объект ответа, который вы можете использовать для получения данных или информации о запросе.

POST(): Функция POST() используется для отправки HTTP POST-запроса на указанный URL. POST-запросы используются для отправки данных на сервер, например, при отправке формы или создании новых ресурсов. Вы можете указать данные для отправки в теле запроса, а также дополнительные параметры, такие как заголовки или аутентификацию. Функция также возвращает объект ответа.
—

19. Язык программирования R. Визуальный анализ данных. Пакет ggplot2 . Построение столбчатой диагараммы (barplot). 
—
Пакет ggplot2 предоставляет возможности для визуализации данных в R. Для построения столбчатой диаграммы (barplot) можно использовать функцию geom_bar(). 
Пример:
Создание датафрейма с данными
data <- data.frame(
  category = c("A", "B", "C", "D"),
  value = c(10, 20, 15, 25)
)
Построение столбчатой диаграммы
ggplot(data, aes(x = category, y = value)) +
  geom_bar(stat = "identity")

data <- data.frame(category = c("A", "B", "C", "D"), value = c(10, 20, 15, 25)) - это код создает датафрейм с данными, в котором переменная category содержит категории (A, B, C, D), а переменная value содержит числовые значения (10, 20, 15, 25).
ggplot(data, aes(x = category, y = value)) - это функция ggplot(), которая создает новый объект графика. Мы указываем данные, которые будут использоваться для построения графика (data), а также задаем эстетику (aes - aesthetics), где переменная category определяет положение столбцов по оси x, а переменная value определяет высоту столбцов по оси y.
geom_bar(stat = "identity") - это слой графика, который определяет тип геометрии, в данном случае столбчатую диаграмму. Параметр stat = "identity" указывает, что высота каждого столбца будет определяться непосредственно значениями переменной value в данных.
+ - это оператор, который используется для добавления слоев на график.
—

20. Язык программирования R. Визуальный анализ данных. Пакет ggplot2 . Построение диагараммы рассеяния (scatterplot). 
—
Пакет ggplot2 также позволяет строить диаграммы рассеяния (scatterplot). Для этого можно использовать функцию geom_point(). Вот пример кода:

# Создание датафрейма с данными
data <- data.frame(
  x = c(1, 2, 3, 4, 5),
  y = c(10, 20, 15, 25, 30)
)
# Построение диаграммы рассеяния
ggplot(data, aes(x = x, y = y)) +
  geom_point()

data <- data.frame(x = c(1, 2, 3, 4, 5), y = c(10, 20, 15, 25, 30)) - это код создает датафрейм с данными, в котором переменная x содержит числовые значения (1, 2, 3, 4, 5), а переменная y также содержит числовые значения (10, 20, 15, 25, 30).

ggplot(data, aes(x = x, y = y)) - это функция ggplot(), которая создает новый объект графика. Мы указываем данные, которые будут использоваться для построения графика (data), а также задаем эстетику (aes - aesthetics), где переменная x определяет положение точек по оси x, а переменная y определяет положение точек по оси y.

geom_point() - это слой графика, который определяет тип геометрии, в данном случае точечную диаграмму. Функция geom_point() используется для добавления точек на график, где каждая точка будет иметь координаты, определенные переменными x и y в данных.
+ - это оператор, который используется для добавления слоев на график.
—

21. Язык программирования R. Визуальный анализ данных. Пакет leaflet . Отображение карты. 
—
Пакет leaflet предоставляет средства для отображения карт в R.
Пример:
Создание карты
map <- leaflet() %>%
  addTiles() %>%
  addMarkers(lng = -73.97, lat = 40.78, popup = "New York City")
Отображение карты
map
это код создает новый объект карты. Функция leaflet() создает пустой объект карты. Затем оператор %>% используется для последовательного добавления слоев на карту. Функция addTiles() добавляет слой базовых тайлов на карту, что обеспечивает фоновое отображение карты. Затем функция addMarkers() используется для добавления маркера на карту. Указанные значения lng и lat определяют долготу и широту маркера, а параметр popup задает текст, который будет отображаться при наведении на маркер.
map - это код, который отображает созданную карту. После выполнения этого кода будет отображена интерактивная карта с базовыми тайлами и маркером "New York City" в указанных координатах.
—

22. Понятие этапа очистки данных, нормализация данных, “аккуратные данные”.
—
Этап очистки данных включает в себя процесс преобразования данных в "аккуратный" формат, устранение ошибок, пропусков и выбросов, а также приведение данных к соответствующим форматам. Нормализация данных - это процесс приведения данных к общему масштабу или диапазону, чтобы они были сопоставимы. "Аккуратные данные" - это данные, которые легко анализировать и использовать без предварительной обработки. Это включает в себя правильное представление данных, отсутствие дубликатов, непротиворечивое представление переменных и обработку пропущенных значений.
—
23. Форматы хранения данных. Форматы CSV, TSV. 
—
CSV (Comma-Separated Values) - это текстовый формат, в котором значения разделены запятыми. Каждая строка представляет собой отдельную запись, а значения могут быть заключены в кавычки. CSV широко используется для обмена данными между различными программами.
TSV (Tab-Separated Values) - это текстовый формат, в котором значения разделены табуляцией. Подобно CSV, каждая строка представляет собой отдельную запись, и значения могут быть заключены в кавычки.
—

24. Форматы хранения данных. Форматы JSON, Parquet. 
—
JSON (JavaScript Object Notation) - это формат хранения данных, основанный на синтаксисе JavaScript. Он использует простой текстовый формат для представления структурированных данных в виде пар "ключ-значение". JSON широко применяется для передачи данных по сети и хранения данных в файловом формате.
Parquet - это колоночный формат хранения данных, разработанный для эффективного сжатия и обработки больших объемов данных. Parquet предоставляет компактное представление данных и поддерживает разделение на блоки и сжатие. Он широко используется в системах Big Data для хранения и анализа данных.
—

25. Инструменты командной строки операционной системы Linux для поиска информации в текстовых файлах.
—
grep - это команда, которая позволяет искать текстовые строки в файлах или потоках данных. Она может быть использована для поиска по определенному шаблону или регулярному выражению.
find - это команда, которая позволяет искать файлы и директории на основе различных критериев, таких как имя, размер, время изменения и т.д.
awk - это утилита для обработки текстовых данных, которая позволяет выполнять различные операции, такие как поиск, фильтрация, преобразование и агрегация данных.
sed - это потоковый редактор, который позволяет выполнять различные операции над текстом, такие как поиск и замена, удаление строк и т.д.
—

26. Понятие больших данных. Файловые форматы хранения данных. 
—
Большие данные (Big Data) - это термин, используемый для описания объема данных, которые слишком велики для традиционных методов обработки данных. Большие данные характеризуются высокой скоростью генерации, разнообразием и сложностью данных.
Файловые форматы хранения данных для больших данных включают Parquet, JSON. Эти форматы оптимизированы для эффективного хранения и обработки больших объемов данных, обеспечивая компактное представление, разделение на блоки, сжатие и быстрый доступ к данным.
—

27. Работа с большими данными в R. Подход lazy-loading. Пакет vroom .
—
В R для работы с большими данными используется подход "ленивой загрузки" (lazy-loading). Это означает, что данные загружаются по мере необходимости, а не все сразу. Такой подход позволяет экономить использование оперативной памяти и обрабатывать данные, которые не помещаются полностью в память.
Пакет vroom является инструментом для эффективной загрузки больших данных в R. Он использует подход ленивой загрузки и может загружать данные из различных форматов, таких как CSV, TSV и файлы фиксированной ширины. Пакет vroom предоставляет функции для быстрой загрузки и обработки данных, а также поддерживает параллельную обработку.
— 

28. Программная платформа работы с большими данными Apache Arrow: концепция организации вычислений. i2z1.ddslab.ru/IAMCTH 3 
—
Apache Arrow - это программная платформа для эффективной обработки больших данных. Она предоставляет средства для организации вычислений и обмена данными между различными системами и языками программирования.
Концепция организации вычислений в Apache Arrow включает использование колоночного формата хранения данных, векторизованных операций и оптимизации памяти. Колоночное хранение позволяет эффективно сжимать и обрабатывать данные, векторизованные операции позволяют выполнять операции над данными сразу для целых столбцов, а оптимизация памяти уменьшает накладные расходы на хранение и обработку данных.
—

29. Работа с большими данными. Колоночные форматы хранения данных. SIMD. 
—
Колоночные форматы хранения данных - это форматы, в которых данные хранятся по столбцам, а не по строкам. Это позволяет сжимать и обрабатывать данные более эффективно, особенно для аналитических запросов, которые часто оперируют с отдельными столбцами.
SIMD (Single Instruction, Multiple Data) - это технология, которая позволяет одному процессору выполнять одну инструкцию над несколькими данными одновременно. SIMD используется для ускорения операций с данными, таких как сжатие, фильтрация, агрегация и т.д. SIMD может быть эффективно применен при работе с колоночными форматами хранения данных, так как операции выполняются над целыми столбцами.
—

30. Работа с большими данными. OLAP СУБД: концепция организации вычислений, примеры программных продуктов. 
—
OLAP (Online Analytical Processing) СУБД (системы управления базами данных) - это специализированные программные продукты для обработки и анализа больших объемов данных. Они предназначены для выполнения аналитических запросов, а не транзакционной обработки данных.
Концепция организации вычислений в OLAP СУБД включает использование многомерных структур данных, таких как кубы и срезы, агрегирование данных, предварительное вычисление и кэширование результатов запросов.
Примеры программных продуктов OLAP СУБД включают Oracle Essbase, Microsoft SQL Server Analysis Services.. Эти системы предоставляют инструменты для моделирования данных, создания отчетов, анализа и визуализации больших объемов данных.

—

Практические задания 
1. Используя пакет dplyr, загрузить датасет iris, отфильтровать и вывести на экран все экземпляры записей для которых параметр Sepal.Width больше или равен 3.2. 
```
library("dplyr")
data("iris")
filter_data <- iris %>%
  filter(Sepal.Width >= 3.2)
filter_data
```

2. Используя пакет dplyr, загрузить в него датасет iris, отфильтровать и вывести на экран все экземпляры записей для столбца Species равные ‘setosa’. 
```
library("dplyr")
data("iris")
filter_data2 <- iris %>%
  filter(Species == "setosa")
filter_data2
```

3. Используя пакет dplyr, импортируйте встроенныйо датасет starwars. Средствами dplyr найти самого высокого персонажа. 
```
library("dplyr")
data("starwars")
tallest_hero <- starwars %>%
  filter(height == max(height, na.rm = TRUE))
cat(tallest_hero$name, tallest_hero$height)
```

4. Используя пакет dplyr, импортируйте встроенный датасет starwars. Средствами dplyr подсчитать ИМТ (индекс массы тела) для всех персонажей. ИМТ подсчитать по формуле \(I=\frac{m}{h^2}\), где \(m\) – масса (weight), а \(h\) – рост (height).
```
library("dplyr")
data("starwars")
IMT <- starwars %>%
  mutate(IMT = mass / ((height/100)^2)) %>%
  select(name, IMT)
IMT
```

5. Импортируйте датасет в R. Это можно выполнить с помощью jsonlite::stream_in(file()) . Датасет находится по адресу https://storage.yandexcloud.net/iamcth-data/dataset.tar.gz. Определить средствами языка R какое количество хостов представлено в данном датасете? 
```
library("jsonlite")
url <- "https://storage.yandexcloud.net/iamcth-data/dataset.tar.gz"
download.file(url, destfile = tf <- tempfile(fileext = ".tar.gz"), mode = "wb")
temp_dir <- tempdir()
untar(tf, exdir = temp_dir)
json_files <- list.files(temp_dir, pattern="\\.json$", full.names = TRUE, recursive = TRUE)
data <- stream_in(file(json_files))
numbers_of_host <- length(unique(data$host))
cat(numbers_of_host)
```

6. Импортируйте датасеты в R. Датасет находится по адресу https://storage.yandexcloud.net/iamcth-data/P2_4_DNSLog.zip. Восстановить названия столбцов в датасете dns.log , основываясь на содержимом headers.csv , подготвить единый датафрейм. 
id.orig_h,addr,The source IP address of the host that initiated the DNS query. 
id.orig_p,port,The source port of the host that initiated the DNS query.
id.resp_h,addr,The destination IP address, typically the DNS server, that received the DNS query.
id_resp_p,port,The destination port number, usually port 53 for DNS, on the server that received the DNS query
```
data = read.csv("dns.log", header = FALSE, sep="\t", encoding = "UTF-8")
header = read.csv("header.csv", encoding = "UTF-8", skip = 1, header = FALSE, sep = ',')$V1
colnames(data) = header
```

7. Импортируйте датасеты в R. Датасет находится по адресу https://storage.yandexcloud.net/iamcth-data/P2_4_DNSLog.zip. Найти топ-10 участников сети, проявляющих наибольшую сетевую активность. 
```
library("dplyr")
data = read.csv("dns.log", header = FALSE, sep="\t", encoding = "UTF-8")
header = read.csv("header.csv", encoding = "UTF-8", skip = 1, header = FALSE, sep = ',')$V1
colnames(data) = header
top_activity <- data %>%
  group_by(ip=`id.orig_h `) %>%
  summarise(activity = n()) %>%
  arrange(desc(activity)) %>%
  head(10)
ip <- select(top_activity,ip)
ip
```

8. Построить график плотности распределения параметра Sepal.Width в R с помощью пакета ggplot2. В качестве данных использовать встроенные датасет iris. 
```
library("ggplot2")
data(iris)
ggplot(data = iris, aes(x = Sepal.Width)) +
  geom_density(fill = "red", alpha= 0.5) +
  labs(x = "Sepal Width", y = "Density") +
  ggtitle("Density Plot of Sepal Width")
```
9. Построить столбчатую гистограмму в R с помощью пакета ggplot2. В качестве данных использовать встроенные датасет iris. 
```
library("ggplot2")
data(iris)
ggplot(data = iris, aes(x = Sepal.Width)) + 
  geom_histogram(binwidth = 0.2, fill = "red", color = "black") +
  labs(x = "Sepal Width", y = "Count") +
  ggtitle("Histogram of Sepal Width")
```

10. Используя R, определить самый высокогорный аэропорт в датасете nycflights13::airports.
Присваиваем переменной alpine_airport результат работы функций: arrange(desc(alt)), которая сортирует строки в порядке убывания значения столбца alt (высота над уровнем моря); head(1), которая возвращает первую строку (то есть аэропорт с наибольшей высотой над уровнем моря) и выводим на экран.
```
library(nycflights13)
alpine_airport <- airports %>%
  arrange(desc(alt)) %>%
  head(1)
cat(alpine_airport$name)
```

11. Используя R, определить самый северный аэропорт в датасете nycflights13::airports.
Присваиваем переменной N_airports значение работы функций: arrange(desc(lat)), которая сортирует строки в порядке убывания результат столбца lat (широта); head(1), которая возвращает первую строку (то есть аэропорт с наибольшей широтой) и выводим результат работы на экран.
```
library(nycflights13)
N_airports <- airports %>%
  arrange(desc(lat)) %>%
  head(1)
cat(N_airports$name)
```
